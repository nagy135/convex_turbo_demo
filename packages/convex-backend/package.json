{
  "name": "@repo/convex-backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "docker compose up",
    "dev:convex": "convex dev",
    "setup": "convex dev --until-success",
    "typecheck": "tsc --noEmit -p convex",
    "deploy": "convex deploy",
    "generate": "convex codegen",
    "admin-key": "docker compose exec backend ./generate_admin_key.sh"
  },
  "dependencies": {
    "convex": "^1.17.4"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
